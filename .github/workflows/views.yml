name: Boost Profile Views

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -s -o /dev/null "https://komarev.com/ghpvc/?username=BikramGole"
